# amp-acp Environment Variables
# Copy this file to .env and customize as needed

# Path to Amp CLI binary (default: amp)
AMP_EXECUTABLE=amp

# Set to 1 to use system Amp instead of npx version
AMP_PREFER_SYSTEM_PATH=1

# Prompt timeout in milliseconds (default: 600000 = 10 min)
AMP_ACP_TIMEOUT_MS=600000

# Nested tool display mode: 'inline' or 'separate'
AMP_ACP_NESTED_MODE=inline

# Logging configuration (optional)
# AMP_ACP_LOG=/path/to/logfile.log
# AMP_ACP_LOG_LEVEL=debug
# AMP_ACP_LOG_FORMAT=json
# DEBUG=acp:*
